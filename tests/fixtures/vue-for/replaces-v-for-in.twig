<div class="{{ class|default('') }}" style="{{ style|default('') }}">
  {% for item in 1..5 %}
    <div>
      {{ item.text }}
    </div>
  {% endfor %}
  {% for n in 1..10 %}<span>{{ n }}</span>{% endfor %}
</div>
